import React from 'react';

import { Container } from './styles';

export const Login: React.FC = () => {
  return (
    <Container>
      <div>login</div>
    </Container>
  );
};

export default Login;
